# KSP CLI — Python Global Installer (`pip install ksp-cli`)

This package is the official Python global distribution installer for the **Kush Secure Protocol (KSP)** native CLI.

## Installation

```bash
pip install ksp-cli
# or with uv
uv tool install ksp-cli
```

Once installed, simply run `ksp` anywhere in your terminal!
